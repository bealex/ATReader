# 2026-09-09

## A number that hid two hundred and fifty-six different books

The first full Litres run reported 259 added and 258 failed, every failure reading `Litres.LitresError
error 0`. Two faults stacked. `LitresError` had no `description`, so a Swift enum reported its own case
index and threw away the status it came with. And the file listing decoded strictly, so one entry with
a field of an unexpected shape threw, and the throw cost the whole book rather than the entry.

`Lenient<Value>` now drops a field it can't read and keeps the rest, `malformed` carries what went
wrong, and the next run brought 516 of 517 across. The one that didn't is a genuine 404: the service
no longer hands that work out.

## The file list decides what's readable, not the type number

Five works were passed over for having `art_type 4`, a number nobody has established the meaning of,
and they were never asked about. Now every work in the library is asked what files it offers, and an
FB2 is what makes it readable. A work without one is reported as such, with its `art_type` recorded
next to it, which is how that number will eventually come to mean something.

The cost is one listing request per work that turns out not to be a book, at 400ms apiece. In a library
of five hundred, that's five requests.

## Books bought elsewhere arrive read

A book from Litres was bought and read before the app ever saw it, so it now lands with its progress
set. Libraries already synchronised were fixed in place by a migration at `user_version` 2, which runs
after `repairProgress()`, since running before it would have its work undone.

## Backing the library up to a folder

Books imported from a file live nowhere else. The store stays out of the device's own backup
deliberately, which means losing the device loses them, so `LibraryArchive` writes the whole thing to a
folder the reader picks: the database, the kept files, and a manifest.

The database goes over by `VACUUM INTO` rather than a file copy. It's open while the backup runs, and a
copy taken behind SQLite's back can catch it mid-write and carry a journal it no longer has. What comes
out is one tidy file with nothing alongside it.

Restoring closes the store, swaps the file and its `-wal` and `-shm` companions, and replaces the books
directory. The next question asked of the store opens it again, so a restore doesn't need a restart.

The folder is remembered as a security-scoped bookmark, since a URL a picker hands over stops working
the moment its scope is given up.

## A glyph has no baseline

The tick on a read row sat below the volume number and the title beside it. Not an offset gone wrong:
`Image` carries no baseline at all, so `RowStack`, which aligns on `.firstTextBaseline`, falls back to
the image's bottom edge. Nothing in the row could have lined it up.

`LineGlyph` sets the symbol as text, which puts it on the line and sizes it from the row's own font. It
sits beside `sitsOnTheLine()` as the glyph half of the same rule: a boxed label hangs below the line
because of its padding, and a symbol sits wherever its box lands because it has no line to sit on.

## Gaps fold the way read books do

A run of volumes the reader doesn't hold is four lines saying the same thing, exactly like a run of
books they have read, so the fold now takes either. One rule came with it: a run is one thing
throughout, so read books and gaps that meet each other fold into two lines rather than one line
claiming to be both.

## Two copies of one book

Two copies of one book sat on the shelf, one carried in by hand and one brought across from Litres. The
pairing compares titles, and those two copies state different volume numbers, which is the very signal
that says "two books, leave them alone". No comparison of names could have paired them.

What can is the text itself. Every local book records what its own text hashes to, so copies that hash
alike are one book whatever either file is called, and the copy the reader has got further into is the
one kept. That pass runs before names are looked at.

The cause was separate: the manual import path never checked the content hash, so a book already
brought across from a service made a second row when the same file was picked by hand. It checks now,
except when re-importing a book already on the shelf, where the text is on the device by definition and
the check would stop it being parsed again at all.

## What a cover says, and what a row says

A cover now carries where the book came from and whether its author is still writing it; the row below
carries where the reader stands in it. Ongoing left the pill row on that reasoning, since it is a fact
about the book rather than about the reading of it.

Likes went behind a setting, off unless asked for. A shelf is what the reader owns and where they are
in it, and a popularity count is the service talking about other people.

## A book nought at the head of every series

A series the reader assembled by hand opened with a book nought, and every volume after it sat one
below where it belonged. `store(series:workIds:)` filed each book under its place in the list it was
given, counted from zero the way `enumerated()` counts, and that place is read back as the book's
volume and drawn as one. A place and a volume are the same figure here, and a series counts from one.

It surfaced only once the shelf stopped holding two copies of a book. Filing rejects a series where two
books claim one volume, so a card with duplicates fell back to numbering by title and never showed the
stored figures at all.

Three parts to it: new series file from one, series already filed move up by one at `user_version` 3,
and a stated volume of nought is now read as no volume at all, since a figure that cannot be a place in
a series says nothing about where a book stands.

## A series stands on its own shelf

The series card was a list of rows: a cover for the book being read, a line each for the ones behind it,
and a folded run standing in for four or more of those. It is now a shelf. Every book is on it in both
of the card's two ways of standing, and what changes is how much room a book the reader is done with
takes up.

Covers for what is left to read; spines for what is behind them. A spine is a fifth of a cover's width,
so a fourteen-book series read to its end no longer pushes the next card off the screen. Tapping the
title switches between the two, each book flying from where it stood to where it is going, and a volume
the reader doesn't hold keeps its place in the run as a grey slot carrying only its number.

The fold went with the rows: `FoldedRun`, `ShelfRow`, the open runs and the eleven tests that held them
up. The spines say what the fold was for and say it without hiding anything.

## What a spine is made of

A blurred cover for colour, and everything else is shading. The corner radius turned out to be the wrong
instrument: a book block is square-cut, and what rounds a spine is the light falling across it. So the
radius is barely there, and the shape comes from a groove down each hinge, a pale head where the paper
shows, a dark foot, and a highlight down the middle.

Lightness is clamped rather than scaled. A ceiling on a dark shelf and a floor on a light one, taken per
channel with `.darken` and `.lighten`, so a cover keeps its hue and gives up only what stood past the
line. Taking a pale cover down with black alone walks the colour out of it, which is what made the first
attempt read as grey strips.

The writing is stamped rather than printed: a dark edge below each letter and a light one along its top.
Both offsets are given in the writing's own space, which the spine then turns on its side, so what is
written as sideways lands as up and down.

## Two bugs behind the same cache

`CoverImages` is an `NSCache` holding what has been decoded this run. A spine read from it but never
wrote to it, so it drew grey until something else on the shelf happened to load the same picture. Then
it drew grey again after every trip to the background, because iOS empties an `NSCache` there and the
spine held no reference of its own. Both the spine and the cover now keep what they draw.

## Covers fill the row

A fixed cover width leaves a ragged margin down the right that changes with the width of the screen. The
size wanted now decides how many covers go in a row, the count is rounded to a whole number of them, and
the width is shared out between that many. Measured once for the whole shelf, and measured before the
padding goes on: reading it afterwards hands the padding back and lays every cover out too wide.

## What was hiding the duplicates

Three separate things kept two copies of one book apart, and each hid the next.

The lookup key was the title *and* the author, so copies whose libraries spell a name differently never
met to be compared. The author is compared afterwards now, by the names the two lines share: one name in
common settles it where one side is a single name, otherwise two are needed, which keeps two same-titled
books by different authors apart.

The volume guard fired on the series aside itself, so a copy carrying `(Name-1)` and a copy without it
were read as different volumes. It now needs both titles to carry asides that actually differ.

And a hand-arranged series files each book's place in the list, which the guard read as a stated volume:
two copies given two places looked like two books, which is the pair that most needed joining.

Underneath all three, the shelf and the series screen disagreed, because the screen read the store
straight. It runs the same pairing now.

## The numbering a card threw away

A series the reader assembled kept its own order, and the card took that to mean it should ignore what
the titles say. But the numbering is the only thing that can say a volume is *absent*: positions counted
off the rows have no gaps in them, since every row has one. It is read for every series now. The order
the reader arranged is untouched; the numbers and the gaps are the titles' own.

## The library is the authors, and the series are runs on their shelves

A card an author, holding their series one after another, and after them the books of theirs that
belong to no series. A reader follows writers more than they follow series, and a card apiece scattered
one writer's several series down the screen in whatever order their newest books arrived.

Under each run is a line saying where the series starts and ends: it ticks upward at the outer edge of
the first book and the last, and where a run carries on to the next row the end is left open, so an
unclosed end reads as "continues". The name sits over the line, masked against the card, and is set in
full only where the line has room for it: failing that its initials, failing that nothing at all. A line
too short even for initials still says where the run is, which is most of what a line is for.

The rows are broken by hand rather than by a flow layout. A bracket has to know which books landed on
which row and how wide they came out, and a layout that only places its subviews cannot say.

## What a service says about a series, and what a file says

Two services number one series two ways, and the disagreement came out on the shelf as volumes claimed
twice with others missing. The reader's own backup settled it: every book from the service is filed one
below the volume its own title states, and the same book from a file is filed at the volume itself.

**author.today numbers a series from zero.** The service's figure is raised by one as it arrives, so
both sources mean the same thing by the same number.

A file states its volume in a `<sequence>`, which the parser only read inside `title-info` and only the
first one named. A file often repeats the sequence under `publish-info`, and sometimes only the repeat
carries the number, so the number is now taken from whichever copy of that sequence has one.

## Which numbering to believe

Neither source can simply outrank the other. Two parts of one volume rightly share a figure, so a
stated numbering with a repeat in it is not wrong; and two services filing one series disagree often
enough that what they state comes out with holes and repeats that mean nothing.

What wins is whichever numbering gives every book a volume of its own, since that one accounts for all
of them: the titles where they tell the books apart, then what the books state, then the titles anyway.
A reader's own arrangement beats all three, because they said so.

## An index the reader gave, and an index the book states

Filing books as a series used to write a place for each of them, over the top of the volume the book
states about itself. So merging two runs looked like an arrangement the reader had made, and stood in
front of what the books say.

The two are separate now. ``Book/seriesOrder`` is what the API or the file states; ``Book/shelfOrder``
is where the reader dragged it. Filing books as a series says which books belong together and nothing
about their order. Only dragging writes a place, which is what makes "every book here carries one"
worth trusting: where they all do, the reader put them there on purpose.

A migration at `user_version` 4 rebuilds `book_series` so its order column can hold nothing at all,
which clears every place filed under the old rule.

## Holding two of something together

Two writers' runs of one story, and one writer reaching two services under two spellings of their name.
Nothing either service says puts them together, so the reader says it and the store keeps it: series in
`book_series`, names in `author_alias`. One dialog serves both, since both are the same act.

A series held together out of several writers' runs stands on a card of its own headed by all of them.
Filed under whichever of them wrote the most of it, it would sit among that writer's own books as
though the others had no part in it.

## Two copies of one book, and the file each brought

The shelf pairs two copies and shows one. The other stayed on the device with its own file, and in
every backup taken of it: a third of the reader's library, by count. Every book whose text the device
already holds under another number is now dropped at launch, with its file, its cover and its pictures.
The copy read furthest into is the one that stays, since the words are identical and the place in them
is all there is to choose between.

The backup itself is brought into step rather than thrown away and written again. It was deleting the
whole of a directory running to hundreds of megabytes and copying it back, which is slow and leaves the
backup empty while it runs.

## A spine in two lights

The spine is a blurred cover and shading, and both had to be told apart by scheme in the end.

Holding a colour to a floor per channel is how a colour is drained of itself: a deep red held to three
quarters comes out the same grey as a deep blue, since each of its channels is raised to the same
figure. On a light shelf the floor now sits low enough to catch only what is nearly black, and the
lifting is done by brightness, which moves a colour without flattening it.

The lit band down the middle is wider and stronger in that light, because there it is also the paper the
writing sits on: a dark title down a coloured spine has nothing else to stand against.

## A book that turns instead of fading

The shelf swapped a spine for a cover by cross-fading one into the other, which says nothing about what
a book is. `Fold` turns it instead: the cover hangs off the spine's right edge, folded into the screen
where it can't be seen, and the pair turns about the spine's left edge until the cover is flat to the
reader and the spine has gone edge-on.

Both panels are projected separately from one shared eye standing in front of the hinge, because a
`rotation3DEffect` nested inside another is flattened between the two and the joint comes apart. Each
panel is a plane, so its projection is a homography and `ProjectionTransform` carries it exactly: place
the panel in space, translate to the eye, divide, translate back.

The turn is one number. `Fold` is `Animatable` on it, so every frame recomputes the room the pair takes,
the shading on each panel and both projections together. The alternative, a `GeometryEffect` carrying
the transform while the frame width and the shading animate on their own, leaves the three disagreeing
in the middle of the turn: the drawn width is a cosine of the turn and an animated frame is a straight
line between its ends, which is 8pt apart at the halfway point of a 48pt cover.

## A transform that flattens what it is given is dropped

The first fold drew the cover full size beside the spine at rest. The projection at that moment is
correct and collapses the cover onto a vertical line, which makes the matrix singular, and SwiftUI
renders the view untransformed rather than not at all. Only the endpoints are affected: a panel a
thousandth of a turn off flat draws properly.

Every panel is now held a twentieth of a point off flat, in whatever angle that works out to for its
own width. `FoldMotionUITests` measures the painted screen at both ends of the turn, since the layout
frame was right the whole time and only the drawing was wrong.

## The shelf turns its books now

`AuthorShelfView` gives every book a `Fold` instead of choosing between a spine and a cover, and
`switchMode` runs it on `FoldMotion.turning`. A book that keeps its place turns where it stands. One
the refold moves to another row is still a book leaving one row and a different book arriving in
another, as far as SwiftUI is concerned, so it keeps its matched geometry and carries its position
across, but it arrives already turned.

Only the panel facing the reader is built. Both of them behind every book would draw the shelf twice
over, since a spine is a blurred, resaturated copy of the same cover, and a library of five hundred
books cannot afford one nobody sees. The turn's own frames are the only place both exist, which the
per-frame body of an `Animatable` view makes possible.

## A cover is a board, not a card

Covers were cut with a corner of a twelfth of their width, which reads as a card. They are square now,
on the same barely-there radius a spine takes, and the few points nearest the binding carry the same
darkening a spine has at each of its edges. `Board` holds those two figures for both, since an edge
drawn darker on one than on the other reads as two different books standing together.

## The library goes to UIKit, so every book turns

A book that lands in a different row when a run refolds is the same book, and SwiftUI could not be told
so: the shelf broke its own rows and each row held its own `ForEach`, which made a book moving between
them one book leaving and a different one arriving. Matched geometry carried it across, but it arrived
already turned, and only the books that kept their place ever turned at all.

The library is now a `UICollectionView` with a section per author, `AuthorCardView` for the cell and
`ShelfView` inside it standing books where `ShelfLayout` puts them. Every book keeps its view for as
long as it is on that shelf, so a refold moves the books that were already there and turns them where
they stand. `ShelfLayout` is a value type in the design system with ten tests of its own, since the row
breaking, the standing heights and the brackets are the whole of the card's geometry, and none of it
could be asked of anything before there was something on screen to ask.

The books are drawn rather than hosted. A spine is a blurred, resaturated copy of its own cover, which
as a live filter costs a filter per book; `SpinePrint` draws each one once into a picture and keeps it.
`MarkPrint` does the same for the handful of marks a cover carries, rounding a book's progress to a
fortieth so a library shares its pictures. What was `.thinMaterial` behind each mark is a translucent
colour: a live blur behind every mark on every cover is a filter per book again, and at eighteen points
the two cannot be told apart.

`Hinge` came through untouched. It was already a value producing a `CATransform3D`, so a `CALayer` uses
the same maths a `projectionEffect` did, and `BookView` animates the turn as a custom animatable layer
property. That is what makes it interruptible: a turn started while another is running picks up wherever
the last had reached, since what is interpolated is the number rather than the matrices.

## A menu written once

The shelf's menus are UIKit now and the rows' are still SwiftUI, so the actions are described as `Deed`
values and set out twice. Written out twice instead, they would drift, and the reader would find a
different menu depending on which of the two they pressed.

## The zoom needed a stand-in

A book grows out of its own cover on the way into the reader, and a zoom grows out of a SwiftUI view.
There is no SwiftUI view of a book any more. The shelf reports where the tapped cover stands on the
screen, the library lays an empty view over that place carrying the transition source, and pushes a tick
later: a zoom looks for its source as the destination arrives, and one placed in the same breath is not
there yet.

## What a turn is animating, and what kept ending it

The turn broke three times, each time because something else decided when it was over.

It began as a `CASpringAnimation` on a custom animatable layer property. Then
`ShelfView.layoutSubviews` snapped every book to its target on every pass, and turning changes how
tall a card is, so something laid the shelf out again the moment a turn started and cancelled the
turn it had just begun. Then the flag that was meant to stop that snapping was itself set during
layout, so by the time `turn(to:animated:)` arrived its guard saw nothing to do and fell through to
the path that doesn't animate.

It is one clock now. `ShelfView` runs a `CADisplayLink` for the whole card, and on every tick it
works out each book's turn and each book's frame from the elapsed time on a critically damped curve.
Nothing is handed to an animator, so nothing can cancel it: a layout in the middle of a turn reads
the clock and puts the books where the turn has got to. `ShelfTurnTests` measures it, since a turn
that snaps and a turn that runs are indistinguishable from the code.

A turn started while another is running takes its geometry from where the books actually are rather
than from the last turn's target, which is what left them scattered when the catalogue's specimen
was driven in a loop.

## A card knows its own height, and the layout is told it

Five ways of asking a collection view to carry a card from one height to another all failed:
invalidating inside a batch update, invalidating and then updating,
`setCollectionViewLayout(animated:)`, a diffable reconfigure with `animatingDifferences`, and
per-frame invalidation with the height looked up by index path. A compositional layout recomputes
and lands every card in one frame however the change is made.

The sixth was self-sizing, and it crashed. The card answered from the turn's clock, that disagreed
with the layout, the layout invalidated and asked again, and the clock had moved by then. The
collection view recursed about thirteen passes deep before UIKit tripped its own assertion. A moving
height and self-sizing cannot both be true.

What works is telling the layout. Heights are `.absolute`, `AuthorCardView.height(across:)`
interpolates from the card's own clock, and the clock invalidates the layout each frame. The width
comes from the layout rather than from the card's bounds, which are zero the first time a card is
asked.

## Measuring instead of guessing

Almost every fix here came from a measurement, and the wrong ones came from reasoning about a
screenshot.

Screenshots through XCUITest are useless for motion: a tap does not return until the app is idle, so
the animation is over before the first frame, and each frame takes about a second. Driving the state
from inside the app on a launch argument catches the turn; reading the layer's presentation in a
unit test does not, since there is no render server.

The pixel dumps that settled things: a column across a spine showing a two-pixel spike exactly three
points in, which was the groove; a column showing the head and foot lines landing on two half-lit
rows instead of one solid one; a row across a card at a quarter turn showing the joint as a dark
boundary rather than a pale one; and the reader's own video, where the card's edge sat at one height
for twenty-two frames and then jumped in a single frame.

## The spine takes its treatment from the book

A shelf holds dark books and light ones at once, and what a spine's writing has to stand against is
the picture under it rather than the room. The blurred cover is measured with `CIAreaAverage` before
any of the treatment is applied, and its luminance picks the ink: a dark book gets white writing and
a dark plate whatever mode the device is in. Only a book whose cover has not arrived falls back to
the shelf's own scheme.

Getting the print to match the SwiftUI spine it replaced took four corrections. A picture's extent
is in pixels and its size is in points, and mixing the two cropped a different part of every cover.
The blur sigma is the radius in points times the screen's density. `CGGradient` interpolates
straight RGBA, so a single transparent stop walks the shading through grey on its way to white and
draws a step beside the highlight: each transparent stop is now the colour it fades from, and only
the alpha moves. And a colour held at one value for even a fiftieth of the width draws as a rule
down the spine, which is what the old curve's hard edge stops were.

The grooves went. They were in the code all along but never drawn: `writing` is laid out sideways
before it is turned, which made the enclosing stack as wide as the spine is tall, so `binding`
placed them three points from *that* box's edges. Drawn where the code always said, at a spine's
width, they read as scratches.

## The seam at the hinge

A turned layer is clipped to whole pixels unless it is told otherwise, so both panels had stepped
edges and the joint between them was a hair of nothing. `allowsEdgeAntialiasing` fixes the edges but
not the joint: two abutting antialiased edges composite to about three quarters and still read as a
line.

The edge now reaches a little past the hinge, under the face, and the overrun is divided by how
square the edge stands. A first attempt put the overlap on the face and scaled it by `sin·cos`,
which is zero at the spine end of the turn, and that is exactly where the reader could see it: a
length laid along a board nearly edge-on to the eye covers almost no screen at all.

## Everything else the shelf gained

A cover is a board now: square along the edge it is bound on, rounded at the two corners that are
handled, and creased where it meets the spine. The white line down that edge was this view's own
border, which draws above every sublayer it has and so painted over the crease under it. The border
is a sublayer beneath the hinge instead.

A missing volume is drawn as the book that isn't there would be, a spine's shape and shading on the
edge and a cover's on the face, so it turns with its run. Both faces carry the volume, and so does a
real book's cover, which is what stops the figure vanishing when a book turns face-on.

Books carry their own accessibility again, which the port had dropped: a cover names its book, a
spine says it has been read and which volume it is, and a gap says which volume is not in the
library.

`sitsOnTheLine()` was lifting a boxed label by its padding and forgetting the descender, so the
ground sat about three points under the line rather than on it. It is the view's own height now,
which is what "the ground sits on the line" means, with no figure to keep in step.
